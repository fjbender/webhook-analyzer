# Database
# Get this from MongoDB Atlas: https://cloud.mongodb.com/
# Format: mongodb+srv://username:password@cluster.mongodb.net/webhook-analyzer?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/webhook-analyzer?retryWrites=true&w=majority

# Authentication
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# Your application URL (use localhost for development, your domain for production)
NEXTAUTH_URL=http://localhost:3000

# Encryption (32 bytes hex for AES-256)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Node Environment (use "development" locally, "production" when deployed)
NODE_ENV=development

